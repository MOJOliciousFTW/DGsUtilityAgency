[project]
name = "dgsutilityagency"
version = "0.1.0"
description = "a tool for justifying all your bad decisions"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "rich>=14.2.0",
    "textual>=6.6.0",
]
[project.optional-dependencies]
build = [
  "pyinstaller>=6.17",
]
